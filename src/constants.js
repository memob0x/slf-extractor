// NOTE: see SLF.md

export const STRING_ENCODING = 'ascii';

export const INT_BUFFER_STRING_LENGTH = 256;

export const INT_BUFFER_NUMBER_LENGTH = 4;

export const INT_SLF_BUFFER_ENTRY_LENGTH = 280;

// placed in slf header after lib name (string) and lib path (string)
export const INT_SLF_BUFFER_OFFSET_START_ENTRIES_COUNT = INT_BUFFER_STRING_LENGTH * 2;
